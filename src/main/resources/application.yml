spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reborn?serverTimezone=Asia/Seoul&useSSL=false
    username: root
    password: mssql0713!!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  redis:
    host: localhost
    port: 6379

logging:
  level:
    org.hibernate.SQL: debug
